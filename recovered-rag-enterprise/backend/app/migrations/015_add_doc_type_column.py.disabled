"""
Migration 015: Add doc_type column to document_status

Domain: Document Processing
Description: Add column to store document type (identity card, passport, news, etc.)
Depends on: 002_document_status_schema
"""

from yoyo import step

steps = [
    step(
        """
        ALTER TABLE document_status ADD COLUMN doc_type TEXT DEFAULT 'unknown'
        """,
        """
        ALTER TABLE document_status DROP COLUMN doc_type
        """
    ),
]
